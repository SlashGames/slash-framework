<Window x:Class="BlueprintEditor.Windows.ListInspectorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ListInspectorWindow" Height="300" Width="300">
    <Grid Height="269" VerticalAlignment="Bottom">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="26"/>
            <RowDefinition Height="26"/>
        </Grid.RowDefinitions>
        <ListView Name="ListView" Grid.Row="0" ItemsSource="{Binding Items}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Item}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Name="SpInspector" Grid.Row="1" />
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <Button Width="75" Click="OnAdd">Add Item</Button>
            <Button Width="75" Click="OnRemove">Remove Item</Button>
            <Button Width="150" Click="OnClose">OK</Button>
        </StackPanel>
    </Grid>
</Window>
